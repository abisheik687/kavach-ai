[pytest]
# Test discovery: active tests only (archive contains legacy tests for dead architecture)
testpaths = tests/active tests
# Collect only files matching test_*.py but exclude tests/archive/
collect_ignore_glob = tests/archive/*
# Async support for FastAPI test client
asyncio_mode = auto
addopts = -v --tb=short
pythonpath = .
