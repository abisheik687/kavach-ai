[pytest]
# Internal trace:
# - Wrong before: plain pytest discovery swept up archived legacy suites that target removed modules and broke the live pipeline CI.
# - Fixed now: default discovery is scoped to the rebuilt smoke tests for the active upload-first application.
testpaths = tests/active
